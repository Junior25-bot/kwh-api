spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kwh_db?useSSL=false&serverTimezone=UTC
    username: kwh_user
    password: kwh_pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

server:
  port: 8080

security:
  jwt:
    secret: "clave-super-secreta-para-jwt"
    expirationMinutes: 120