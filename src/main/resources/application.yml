spring:
  datasource:

    url: jdbc:mysql://localhost:3306/kwh_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

    url: jdbc:mysql://localhost:3307/kwh_db?useSSL=false&serverTimezone=UTC

    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081
  address: 0.0.0.0

security:
  jwt:
    secret: "clave-super-secreta-para-jwt"
    expirationMinutes: 120